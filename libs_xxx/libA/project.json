{
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rsbuild build && tsc -p tsconfig.build.json",
        "cwd": "{projectRoot}"
      }
    }
  }
}